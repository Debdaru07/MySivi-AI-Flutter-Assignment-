name: mysivi_ai_assessment
description: "Mini Chat Application built in Flutter for MySivi AI assessment."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.5.1
  http: ^1.2.2
  intl: ^0.19.0
  phosphor_flutter: ^2.1.0
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

  riverpod_test: ^0.1.6

flutter:
  uses-material-design: true
